//Program to find borga(x) given x
#include<stdio.h>
#include<math.h>
int input(int x)
{
  printf("Enter the value of x: ");
  scanf("%d",&x);
  return x;
}
float borga_x(int x)
{
   float s[x],d[x];
   for(int i=0;i<x;i++)
   {
     s[i]=pow(x,i);
   }
   for(int j=0;j<x;j++)
   {
     if(j%2!=0)
     {
       if(int y=1;y<=j;)
     }
   }
}
void output(int x,float result)
{
  
}